import { View } from 'react-native';

export default function NotFoundScreen() {
  // Порожній екран - InitialLayout обробить редірект
  return <View style={{ flex: 1, backgroundColor: '#000' }} />;
}
